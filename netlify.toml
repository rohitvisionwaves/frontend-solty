# Netlify config for Angular frontend (shona)
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist/frontend/browser"

# SPA: serve index.html for all routes so Angular router works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
